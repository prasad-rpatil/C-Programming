#include <stdio.h>

int main(){
    int marks[90];
    marks[0] = 45;
    marks[1] = 95;
    // we can go upto all the way till marks[89]
    printf("Marks 0 and marks 1 are %d %d\n ", marks[0], marks[1]);
    return 0;
}