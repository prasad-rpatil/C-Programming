#include <stdio.h>
int main()
{
    int l,b;
    printf("Enter the length\n");
    scanf("%d", &l);
    printf("Enter the bredth\n");
    scanf("%d", &b);
    printf("The Area of a Rectangle is %d", l*b);
    return 0;
}
